import { Box } from "@mui/material";

const Admindashboard = () => {
  return (
    <Box
      sx={{
        height: "100vh",
        width: "100%",
        display: "flex",
        justifyContent: "center",
      }}
    >
      <Box ml={1}>Admin Dashboard page</Box>
    </Box>
  );
};

export default Admindashboard;
